import React from "react";

const OrderByAlpha = ({ onClick }) => (
    <button className="btn btn-primary me-2" onClick={onClick}>
        Alfabetisk ordning
    </button>
);

export default OrderByAlpha